#include<iostream>
#include<stdlib.h>
using namespace std;
//I DON'T LIKE THIS KOSTEEN.EASY ONLY NO?
int  reduced(int x)//FUNCTION THAT PERFORMS REDUCED MODULO OPERATION
{
    return (x%1000);
}
main()
{
    int cmd,one,two;
    char RAM[1000][3],regs[10][3];
    while(1)
    {
        cout<<endl;
        cin>>cmd>>one>>two;
        switch(cmd)
        {
        case 1:
            exit(100);//1 HALT



        }
    }
}
